# BattleshipBackend

- Built with TypeScript, Fastify and PostgreSQL
- Implements client-server architecture with REST API and WebSocket event handler
- Uses JWT for user authorization

`BattleshipBackend` is the backend part of the **Battleship** project. Unfortunately, this readme is not written yet. Please by now reffer to the `BattleshipFrontend` readme available at https://github.com/Kak0ytachel/BattleshipFrontend
